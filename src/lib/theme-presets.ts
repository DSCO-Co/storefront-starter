/**
 * Storefront re-export of the shared theme-preset library. The presets +
 * matrix live in `@dscodotco/theme-presets` (zero-dep, also consumed by the console
 * theme editor and open-sourceable); this keeps the `@/lib/theme-presets`
 * import path stable for the storefront.
 */
export * from "@dscodotco/theme-presets";
